---
interface Props {
	platform: string;
	username: string;
}
const { platform, username } = Astro.props;
---

<a
	class="px-4 py-2 text-white bg-purple-900 no-underline"
	href={`https://${platform}.com/${username}`}>{platform}</a
>
